{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Hugo",
            "type": "shell",
            "command": "hugo server --gc --bind=0.0.0.0",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}/blog",
            }
        },
    ]
}