[project]
name = "mefi-mod-log-check"
version = "0.1.0"
description = "Check scripts for mefi mod log archive"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.14",
    "curl-cffi>=0.15",
    "dateparser>=1.3",
    "lxml>=6",
]
